# Drag handle
> You can use a custom handler for the desired `Draggable` just by using `drag-handle-selector` and selecting the element throught its `identifier` either it's a class or an id

<doc-example title="Drag handle" file="drag-handle" />
